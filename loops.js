// Sukuriamas masyvas su skaičiais
const numbers = [5, 12, 8, 20, 3, 15, 7];

// Naudojamas for...of ciklas
for (const number of numbers) {
  if (number > 10) {
    console.log(number);
  }
}
